import { colors } from '../../../res';

export const styles = {
  clientCardWrapper: {
    display: 'flex',
    flexDirection: 'row',
    height: 60,
    backgroundColor: colors.clientCardBackground,
    color: colors.white,
    alignItems: 'center',
    justifyContent: 'space-around',
    marginBottom: 8
  }
};

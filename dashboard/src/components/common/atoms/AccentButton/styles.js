import { colors } from '../../../../res';

export const styles = {
  button: {
    fontFamily: 'Montserrat-Bold, sans-serif',
    fontSize: 14,
    color: colors.white,
    backgroundColor: colors.accentButton,
    borderRadius: 0,
    margin: 0,
    textTransform: 'none',
    textAlign: 'left',
    flex: 1,
    display: 'flex'
  }
};
